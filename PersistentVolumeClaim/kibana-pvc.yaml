apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kibana-config-pvc
spec:
  volumeName: kibana-config-pv
  storageClassName: ""
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  